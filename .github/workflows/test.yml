name: Tests
on:
  push:
    branches: ['**']
  pull_request:
  workflow_call:
permissions:
  contents: read
jobs:
  test:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python: ['3.11', '3.12', '3.13']
    defaults:
      run:
        shell: bash
    env:
      PYTHONUTF8: '1'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python }}
      - name: Install uv
        timeout-minutes: 2
        run: python -m pip install uv==0.12.10
      - name: Check locked project resolve
        timeout-minutes: 2
        run: uv lock --check --python '${{ matrix.python }}'
      - name: Check latest dependency resolve
        if: matrix.python == '3.13'
        timeout-minutes: 4
        run: uv lock --upgrade --dry-run --python '${{ matrix.python }}'
      - name: Check direct lower bounds
        if: matrix.python == '3.11' && matrix.os == 'ubuntu-latest'
        timeout-minutes: 5
        run: |
          uv venv /tmp/jotline-lower-bound --python '${{ matrix.python }}'
          uv pip install --python /tmp/jotline-lower-bound/bin/python --strict -e . 'textual==8.2.8' 'pytest==8.2.0' 'pytest-asyncio==0.24.0'
          /tmp/jotline-lower-bound/bin/python -m pytest -q tests/test_cli_redteam.py tests/test_packaging.py
      - name: Sync locked test environment
        timeout-minutes: 4
        run: uv sync --locked --extra dev --extra encryption --python '${{ matrix.python }}'
      - name: Run tests
        timeout-minutes: 8
        run: uv run --locked pytest -q -o faulthandler_timeout=60
      - name: Build distributions
        timeout-minutes: 3
        run: uv build --clear
      - name: Checksum packages and copy installers
        timeout-minutes: 2
        run: python scripts/release_metadata.py --checksums
      - name: Install and exercise the built wheel through the public installer
        timeout-minutes: 4
        run: |
          uv venv .smoke-venv --python '${{ matrix.python }}'
          smoke_python=.smoke-venv/bin/python
          python scripts/install.py --from-dir dist --python "$smoke_python" --installer pip
          "$smoke_python" -I scripts/smoke_install.py
